{ ... }: {
  # NOTE: Add when ladybird browser becomes stable!!
  # home.packages = with pkgs; [ ladybird ];

  # On macOS, Firefox itself is installed via Homebrew — see
  # modules/system/darwin/desktop-apps.nix.
}
